"""Вставка текста на macOS: NSPasteboard + CGEventPost(Cmd+V).

Пока не реализовано — фаза 2 порта (см. docs/macos-port.md).
"""


def insert_text(text, mode):
    raise NotImplementedError(
        "Вставка текста на macOS ещё не реализована (фаза 2 порта)"
    )
