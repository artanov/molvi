# Полная установка для разработки (README): ядро + CUDA-библиотеки для GPU.
# (В установленной сборке CUDA-DLL докачиваются мастером — см. molvi/fetch.py.)
-r requirements-base.txt
nvidia-cublas-cu12
nvidia-cudnn-cu12>=9
